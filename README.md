Created the HTML structure

Added an input box for typing tasks

Added an “Add” button

Added an empty <ul> list to display tasks

Styled the UI with CSS

Centered the app on the page

Styled the input, button, and task list

Added a special style (.completed) to show finished tasks with a line-through

Used JavaScript for interactivity

Added an event listener to the “Add” button

When clicked, it reads the text from the input

Dynamically creates a new <li> element with the task text

Adds a delete button (“X”) inside each task

Implemented task features
✔ Clicking task text toggles completed (adds/removes .completed class)
✔ Clicking the delete button removes the task from the list
✔ Pressing Enter also adds the task

Tested the functionality

Added tasks

Marked tasks complete

Deleted tasks

Verified updates happen instantly without page reload
